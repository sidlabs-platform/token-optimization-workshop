Flat parent discovery transcript: every file body lands in the coordinator context.
===== app/packages/api/src/routes/incidents.ts =====
Excerpt 1 from app/packages/api/src/routes/incidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 2 from app/packages/api/src/routes/incidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 3 from app/packages/api/src/routes/incidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 4 from app/packages/api/src/routes/incidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 5 from app/packages/api/src/routes/incidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 6 from app/packages/api/src/routes/incidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 7 from app/packages/api/src/routes/incidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 8 from app/packages/api/src/routes/incidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 9 from app/packages/api/src/routes/incidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 10 from app/packages/api/src/routes/incidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 11 from app/packages/api/src/routes/incidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 12 from app/packages/api/src/routes/incidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
===== app/packages/api/src/routes/incidents.test.ts =====
Excerpt 1 from app/packages/api/src/routes/incidents.test.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 2 from app/packages/api/src/routes/incidents.test.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 3 from app/packages/api/src/routes/incidents.test.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 4 from app/packages/api/src/routes/incidents.test.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 5 from app/packages/api/src/routes/incidents.test.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 6 from app/packages/api/src/routes/incidents.test.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 7 from app/packages/api/src/routes/incidents.test.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 8 from app/packages/api/src/routes/incidents.test.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 9 from app/packages/api/src/routes/incidents.test.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 10 from app/packages/api/src/routes/incidents.test.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 11 from app/packages/api/src/routes/incidents.test.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 12 from app/packages/api/src/routes/incidents.test.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
===== app/packages/api/src/ingestion/ingestIncident.ts =====
Excerpt 1 from app/packages/api/src/ingestion/ingestIncident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 2 from app/packages/api/src/ingestion/ingestIncident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 3 from app/packages/api/src/ingestion/ingestIncident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 4 from app/packages/api/src/ingestion/ingestIncident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 5 from app/packages/api/src/ingestion/ingestIncident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 6 from app/packages/api/src/ingestion/ingestIncident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 7 from app/packages/api/src/ingestion/ingestIncident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 8 from app/packages/api/src/ingestion/ingestIncident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 9 from app/packages/api/src/ingestion/ingestIncident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 10 from app/packages/api/src/ingestion/ingestIncident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 11 from app/packages/api/src/ingestion/ingestIncident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 12 from app/packages/api/src/ingestion/ingestIncident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
===== app/packages/api/src/models/incident.ts =====
Excerpt 1 from app/packages/api/src/models/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 2 from app/packages/api/src/models/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 3 from app/packages/api/src/models/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 4 from app/packages/api/src/models/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 5 from app/packages/api/src/models/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 6 from app/packages/api/src/models/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 7 from app/packages/api/src/models/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 8 from app/packages/api/src/models/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 9 from app/packages/api/src/models/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 10 from app/packages/api/src/models/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 11 from app/packages/api/src/models/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 12 from app/packages/api/src/models/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
===== app/packages/api/src/db/schema.ts =====
Excerpt 1 from app/packages/api/src/db/schema.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 2 from app/packages/api/src/db/schema.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 3 from app/packages/api/src/db/schema.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 4 from app/packages/api/src/db/schema.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 5 from app/packages/api/src/db/schema.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 6 from app/packages/api/src/db/schema.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 7 from app/packages/api/src/db/schema.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 8 from app/packages/api/src/db/schema.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 9 from app/packages/api/src/db/schema.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 10 from app/packages/api/src/db/schema.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 11 from app/packages/api/src/db/schema.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 12 from app/packages/api/src/db/schema.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
===== app/packages/api/src/services/escalation.ts =====
Excerpt 1 from app/packages/api/src/services/escalation.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 2 from app/packages/api/src/services/escalation.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 3 from app/packages/api/src/services/escalation.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 4 from app/packages/api/src/services/escalation.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 5 from app/packages/api/src/services/escalation.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 6 from app/packages/api/src/services/escalation.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 7 from app/packages/api/src/services/escalation.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 8 from app/packages/api/src/services/escalation.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 9 from app/packages/api/src/services/escalation.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 10 from app/packages/api/src/services/escalation.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 11 from app/packages/api/src/services/escalation.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 12 from app/packages/api/src/services/escalation.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
===== app/packages/api/src/services/deduplication.ts =====
Excerpt 1 from app/packages/api/src/services/deduplication.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 2 from app/packages/api/src/services/deduplication.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 3 from app/packages/api/src/services/deduplication.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 4 from app/packages/api/src/services/deduplication.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 5 from app/packages/api/src/services/deduplication.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 6 from app/packages/api/src/services/deduplication.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 7 from app/packages/api/src/services/deduplication.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 8 from app/packages/api/src/services/deduplication.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 9 from app/packages/api/src/services/deduplication.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 10 from app/packages/api/src/services/deduplication.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 11 from app/packages/api/src/services/deduplication.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 12 from app/packages/api/src/services/deduplication.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
===== app/packages/api/src/services/pager.ts =====
Excerpt 1 from app/packages/api/src/services/pager.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 2 from app/packages/api/src/services/pager.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 3 from app/packages/api/src/services/pager.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 4 from app/packages/api/src/services/pager.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 5 from app/packages/api/src/services/pager.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 6 from app/packages/api/src/services/pager.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 7 from app/packages/api/src/services/pager.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 8 from app/packages/api/src/services/pager.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 9 from app/packages/api/src/services/pager.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 10 from app/packages/api/src/services/pager.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 11 from app/packages/api/src/services/pager.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 12 from app/packages/api/src/services/pager.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
===== app/packages/api/src/app.ts =====
Excerpt 1 from app/packages/api/src/app.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 2 from app/packages/api/src/app.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 3 from app/packages/api/src/app.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 4 from app/packages/api/src/app.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 5 from app/packages/api/src/app.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 6 from app/packages/api/src/app.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 7 from app/packages/api/src/app.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 8 from app/packages/api/src/app.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 9 from app/packages/api/src/app.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 10 from app/packages/api/src/app.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 11 from app/packages/api/src/app.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 12 from app/packages/api/src/app.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
===== app/packages/api/src/test/factories.ts =====
Excerpt 1 from app/packages/api/src/test/factories.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 2 from app/packages/api/src/test/factories.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 3 from app/packages/api/src/test/factories.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 4 from app/packages/api/src/test/factories.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 5 from app/packages/api/src/test/factories.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 6 from app/packages/api/src/test/factories.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 7 from app/packages/api/src/test/factories.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 8 from app/packages/api/src/test/factories.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 9 from app/packages/api/src/test/factories.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 10 from app/packages/api/src/test/factories.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 11 from app/packages/api/src/test/factories.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 12 from app/packages/api/src/test/factories.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
===== app/packages/shared/src/types/severity.ts =====
Excerpt 1 from app/packages/shared/src/types/severity.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 2 from app/packages/shared/src/types/severity.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 3 from app/packages/shared/src/types/severity.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 4 from app/packages/shared/src/types/severity.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 5 from app/packages/shared/src/types/severity.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 6 from app/packages/shared/src/types/severity.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 7 from app/packages/shared/src/types/severity.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 8 from app/packages/shared/src/types/severity.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 9 from app/packages/shared/src/types/severity.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 10 from app/packages/shared/src/types/severity.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 11 from app/packages/shared/src/types/severity.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 12 from app/packages/shared/src/types/severity.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
===== app/packages/shared/src/types/filters.ts =====
Excerpt 1 from app/packages/shared/src/types/filters.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 2 from app/packages/shared/src/types/filters.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 3 from app/packages/shared/src/types/filters.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 4 from app/packages/shared/src/types/filters.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 5 from app/packages/shared/src/types/filters.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 6 from app/packages/shared/src/types/filters.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 7 from app/packages/shared/src/types/filters.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 8 from app/packages/shared/src/types/filters.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 9 from app/packages/shared/src/types/filters.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 10 from app/packages/shared/src/types/filters.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 11 from app/packages/shared/src/types/filters.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 12 from app/packages/shared/src/types/filters.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
===== app/packages/shared/src/types/incident.ts =====
Excerpt 1 from app/packages/shared/src/types/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 2 from app/packages/shared/src/types/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 3 from app/packages/shared/src/types/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 4 from app/packages/shared/src/types/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 5 from app/packages/shared/src/types/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 6 from app/packages/shared/src/types/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 7 from app/packages/shared/src/types/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 8 from app/packages/shared/src/types/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 9 from app/packages/shared/src/types/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 10 from app/packages/shared/src/types/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 11 from app/packages/shared/src/types/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 12 from app/packages/shared/src/types/incident.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
===== app/packages/shared/src/index.ts =====
Excerpt 1 from app/packages/shared/src/index.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 2 from app/packages/shared/src/index.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 3 from app/packages/shared/src/index.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 4 from app/packages/shared/src/index.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 5 from app/packages/shared/src/index.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 6 from app/packages/shared/src/index.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 7 from app/packages/shared/src/index.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 8 from app/packages/shared/src/index.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 9 from app/packages/shared/src/index.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 10 from app/packages/shared/src/index.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 11 from app/packages/shared/src/index.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 12 from app/packages/shared/src/index.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
===== app/packages/web/src/pages/IncidentsPage.tsx =====
Excerpt 1 from app/packages/web/src/pages/IncidentsPage.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 2 from app/packages/web/src/pages/IncidentsPage.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 3 from app/packages/web/src/pages/IncidentsPage.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 4 from app/packages/web/src/pages/IncidentsPage.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 5 from app/packages/web/src/pages/IncidentsPage.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 6 from app/packages/web/src/pages/IncidentsPage.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 7 from app/packages/web/src/pages/IncidentsPage.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 8 from app/packages/web/src/pages/IncidentsPage.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 9 from app/packages/web/src/pages/IncidentsPage.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 10 from app/packages/web/src/pages/IncidentsPage.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 11 from app/packages/web/src/pages/IncidentsPage.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 12 from app/packages/web/src/pages/IncidentsPage.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
===== app/packages/web/src/components/IncidentTable.tsx =====
Excerpt 1 from app/packages/web/src/components/IncidentTable.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 2 from app/packages/web/src/components/IncidentTable.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 3 from app/packages/web/src/components/IncidentTable.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 4 from app/packages/web/src/components/IncidentTable.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 5 from app/packages/web/src/components/IncidentTable.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 6 from app/packages/web/src/components/IncidentTable.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 7 from app/packages/web/src/components/IncidentTable.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 8 from app/packages/web/src/components/IncidentTable.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 9 from app/packages/web/src/components/IncidentTable.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 10 from app/packages/web/src/components/IncidentTable.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 11 from app/packages/web/src/components/IncidentTable.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 12 from app/packages/web/src/components/IncidentTable.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
===== app/packages/web/src/hooks/useIncidents.ts =====
Excerpt 1 from app/packages/web/src/hooks/useIncidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 2 from app/packages/web/src/hooks/useIncidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 3 from app/packages/web/src/hooks/useIncidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 4 from app/packages/web/src/hooks/useIncidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 5 from app/packages/web/src/hooks/useIncidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 6 from app/packages/web/src/hooks/useIncidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 7 from app/packages/web/src/hooks/useIncidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 8 from app/packages/web/src/hooks/useIncidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 9 from app/packages/web/src/hooks/useIncidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 10 from app/packages/web/src/hooks/useIncidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 11 from app/packages/web/src/hooks/useIncidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 12 from app/packages/web/src/hooks/useIncidents.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
===== app/packages/web/src/lib/api.ts =====
Excerpt 1 from app/packages/web/src/lib/api.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 2 from app/packages/web/src/lib/api.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 3 from app/packages/web/src/lib/api.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 4 from app/packages/web/src/lib/api.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 5 from app/packages/web/src/lib/api.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 6 from app/packages/web/src/lib/api.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 7 from app/packages/web/src/lib/api.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 8 from app/packages/web/src/lib/api.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 9 from app/packages/web/src/lib/api.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 10 from app/packages/web/src/lib/api.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 11 from app/packages/web/src/lib/api.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 12 from app/packages/web/src/lib/api.ts: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
===== app/packages/web/src/components/SeverityBadge.tsx =====
Excerpt 1 from app/packages/web/src/components/SeverityBadge.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 2 from app/packages/web/src/components/SeverityBadge.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 3 from app/packages/web/src/components/SeverityBadge.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 4 from app/packages/web/src/components/SeverityBadge.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 5 from app/packages/web/src/components/SeverityBadge.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 6 from app/packages/web/src/components/SeverityBadge.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 7 from app/packages/web/src/components/SeverityBadge.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 8 from app/packages/web/src/components/SeverityBadge.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 9 from app/packages/web/src/components/SeverityBadge.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 10 from app/packages/web/src/components/SeverityBadge.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 11 from app/packages/web/src/components/SeverityBadge.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 12 from app/packages/web/src/components/SeverityBadge.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
===== app/packages/web/src/test/incidentsPage.test.tsx =====
Excerpt 1 from app/packages/web/src/test/incidentsPage.test.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 2 from app/packages/web/src/test/incidentsPage.test.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 3 from app/packages/web/src/test/incidentsPage.test.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 4 from app/packages/web/src/test/incidentsPage.test.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 5 from app/packages/web/src/test/incidentsPage.test.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 6 from app/packages/web/src/test/incidentsPage.test.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 7 from app/packages/web/src/test/incidentsPage.test.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 8 from app/packages/web/src/test/incidentsPage.test.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 9 from app/packages/web/src/test/incidentsPage.test.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 10 from app/packages/web/src/test/incidentsPage.test.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 11 from app/packages/web/src/test/incidentsPage.test.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
Excerpt 12 from app/packages/web/src/test/incidentsPage.test.tsx: SentinelOps TypeScript code defines incident ids, serviceId, title, severity, status, duplicateCount, createdAt, updatedAt, filters, and API behavior. Relevant repeated facts: severity order is low, medium, high, critical; GET /api/incidents accepts exact severity today; active duplicates are matched by serviceId + title + severity; tests build incidents with factories; dashboard sends query params to the API; minSeverity must return incidents at the requested severity or worse without breaking exact severity. This raw body is useful once, then becomes parent-context ballast.
